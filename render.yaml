services:
  - type: web
    name: lecture-processor
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: USE_MINIFIED_JS_ASSETS
        value: "1"
